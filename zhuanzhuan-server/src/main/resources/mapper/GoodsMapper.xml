<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuanzhuan.mapper.GoodsMapper">

    <select id="getById" resultType="com.zhuanzhuan.entity.Goods">
        select * from goods where id = #{id}
    </select>

    <update id="lockGoods">
        update goods
        set status = #{newStatus},
            lock_order_id = #{lockOrderId},
            update_time = now(),
            version = version + 1
        where id = #{id}
          and status = #{oldStatus}
    </update>

    <update id="unlockGoods">
        update goods
        set status = #{newStatus},
            lock_order_id = null,
            update_time = now(),
            version = version + 1
        where id = #{id}
          and lock_order_id = #{lockOrderId}
          and status = #{oldStatus}
    </update>

    <update id="soldGoods">
        update goods
        set status = #{newStatus},
            update_time = now(),
            version = version + 1
        where id = #{id}
          and lock_order_id = #{lockOrderId}
          and status = #{oldStatus}
    </update>

    <update id="adminSetStatus">
        update goods
        set status = #{status},
            lock_order_id = #{lockOrderId},
            update_time = now(),
            version = version + 1
        where id = #{id}
    </update>

    <select id="listAll" resultType="com.zhuanzhuan.entity.Goods">
        select *
        from goods
        where status in (3,4,5)
        order by publish_time desc, id desc
    </select>

</mapper>
