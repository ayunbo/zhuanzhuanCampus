<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuanzhuan.platform.history.mapper.SearchHistoryMapper">

    <select id="listByUserId" resultType="com.zhuanzhuan.vo.SearchHistoryItemVO">
        select
            id as historyId,
            keyword,
            category_id as categoryId,
            seller_id as sellerId,
            status,
            quality,
            location,
            min_price as minPrice,
            max_price as maxPrice,
            sort_by as sortBy,
            search_count as searchCount,
            last_search_time as lastSearchTime
        from searchhistory
        where user_id = #{userId}
        order by last_search_time desc, id desc
        limit #{limit}
    </select>
</mapper>
