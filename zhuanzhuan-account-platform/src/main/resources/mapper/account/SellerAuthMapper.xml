<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuanzhuan.platform.account.mapper.SellerAuthMapper">

    <resultMap id="BaseResultMap" type="com.zhuanzhuan.entity.SellerAuth">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="realName" column="real_name"/>
        <result property="studentNo" column="student_no"/>
        <result property="phone" column="phone"/>
        <result property="material" column="material"/>
        <result property="status" column="status"/>
        <result property="reason" column="reason"/>
        <result property="auditAdminId" column="audit_admin_id"/>
        <result property="auditTime" column="audit_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createUser" column="create_user"/>
        <result property="updateUser" column="update_user"/>
    </resultMap>

    <sql id="BaseColumns">
        id, user_id, real_name, student_no, phone, material, status, reason, audit_admin_id, audit_time,
        create_time, update_time, create_user, update_user
    </sql>

    <select id="getById" resultMap="BaseResultMap">
        select
        <include refid="BaseColumns"/>
        from sellerauth
        where id = #{id}
        limit 1
    </select>

    <select id="getLatestByUserId" resultMap="BaseResultMap">
        select
        <include refid="BaseColumns"/>
        from sellerauth
        where user_id = #{userId}
        order by create_time desc
        limit 1
    </select>

    <select id="pageQuery" resultType="com.zhuanzhuan.vo.SellerAuthResultVO">
        select
            sa.id,
            sa.user_id,
            u.name as user_name,
            sa.real_name,
            sa.student_no,
            sa.phone,
            sa.material,
            sa.status,
            sa.reason,
            sa.audit_admin_id,
            sa.audit_time,
            sa.create_time,
            sa.update_time
        from sellerauth sa
        left join `user` u on sa.user_id = u.id
        <where>
            <if test="status != null">
                and sa.status = #{status}
            </if>
            <if test="name != null and name != ''">
                and (sa.real_name like concat('%', #{name}, '%')
                or u.name like concat('%', #{name}, '%'))
            </if>
            <if test="phone != null and phone != ''">
                and sa.phone like concat('%', #{phone}, '%')
            </if>
            <if test="studentNo != null and studentNo != ''">
                and sa.student_no like concat('%', #{studentNo}, '%')
            </if>
        </where>
        order by sa.create_time desc
    </select>

    <insert id="insert">
        insert into sellerauth (
        id, user_id, real_name, student_no, phone, material, status, reason, audit_admin_id, audit_time,
        create_time, update_time, create_user, update_user
        ) values (
        #{id}, #{userId}, #{realName}, #{studentNo}, #{phone}, #{material}, #{status}, #{reason}, #{auditAdminId}, #{auditTime},
        #{createTime}, #{updateTime}, #{createUser}, #{updateUser}
        )
    </insert>

    <update id="updateAuditById">
        update sellerauth
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="reason != null">
                reason = #{reason},
            </if>
            <if test="auditAdminId != null">
                audit_admin_id = #{auditAdminId},
            </if>
            <if test="auditTime != null">
                audit_time = #{auditTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="updateStatusByUserId">
        update sellerauth
        set status = #{status}
        where user_id = #{userId}
    </update>

</mapper>
